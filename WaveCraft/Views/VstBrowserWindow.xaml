<Window x:Class="WaveCraft.Views.VstBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VST Browser"
        Width="900" Height="600"
        MinWidth="800" MinHeight="500"
        Background="{StaticResource AbletonBackground}"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <!-- List Item Style -->
        <Style x:Key="PluginListItem" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AbletonTextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource AbletonBorder}"
                                BorderThickness="0,0,0,1"
                                Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AbletonHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource AbletonOrange}"/>
                                <Setter Property="Foreground" Value="{StaticResource AbletonTextHighlight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GridView Column Header Style -->
        <Style x:Key="ColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource AbletonPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AbletonTextSecondary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AbletonBorder}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar: Search & Actions -->
        <Border Grid.Row="0" Background="{StaticResource AbletonPanel}"
                BorderBrush="{StaticResource AbletonBorder}" BorderThickness="0,0,0,1">
            <Grid Margin="12,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Search & Action Buttons -->
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" 
                             Text="{Binding ScanPath, UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center"
                             Height="28" Margin="0,0,8,0"/>

                    <Button Grid.Column="1" Content="Scan Folder" 
                            Command="{Binding ScanDirectoryCommand}"
                            MinWidth="80" Margin="0,0,4,0"/>
                    <Button Grid.Column="2" Content="Browse File" 
                            Command="{Binding BrowseForPluginCommand}"
                            MinWidth="80" Margin="0,0,4,0"/>
                    <Button Grid.Column="3" Content="Scan All" 
                            Command="{Binding ScanAllPathsCommand}"
                            MinWidth="80"/>
                </Grid>

                <!-- Scan Paths Display -->
                <StackPanel Grid.Row="1">
                    <TextBlock Text="SCAN PATHS" 
                               Foreground="{StaticResource AbletonTextSecondary}"
                               FontSize="9" FontWeight="Bold" Margin="0,0,0,4"/>
                    <ItemsControl ItemsSource="{Binding ScanPaths}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource AbletonBackground}"
                                        BorderBrush="{StaticResource AbletonBorder}"
                                        BorderThickness="1"
                                        Padding="6,3" Margin="0,0,4,4">
                                    <TextBlock Text="{Binding}" 
                                               Foreground="{StaticResource AbletonTextSecondary}"
                                               FontSize="9"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content: Folder Tree (30%) + Plugin List (70%) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="70*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Folder Tree (Placeholder) -->
            <Border Grid.Column="0" Background="{StaticResource AbletonPanel}"
                    BorderBrush="{StaticResource AbletonBorder}" BorderThickness="0,0,1,0">
                <StackPanel Margin="12">
                    <TextBlock Text="FOLDERS" 
                               Foreground="{StaticResource AbletonTextSecondary}"
                               FontSize="9" FontWeight="Bold" Margin="0,0,0,8"/>
                    <TreeView Background="Transparent" BorderThickness="0"
                              Foreground="{StaticResource AbletonTextPrimary}">
                        <TreeViewItem Header="VST Plugins" IsExpanded="True">
                            <TreeViewItem Header="All Plugins"/>
                            <TreeViewItem Header="Instruments"/>
                            <TreeViewItem Header="Effects"/>
                        </TreeViewItem>
                    </TreeView>
                </StackPanel>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="1" 
                          Background="{StaticResource AbletonBorder}"
                          HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

            <!-- Right: Plugin List -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Plugin ListView -->
                <ListView Grid.Row="0"
                          ItemsSource="{Binding DiscoveredPlugins}"
                          SelectedIndex="{Binding SelectedIndex}"
                          Background="{StaticResource AbletonBackground}"
                          Foreground="{StaticResource AbletonTextPrimary}"
                          BorderThickness="0"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ItemContainerStyle="{StaticResource PluginListItem}">
                    <ListView.View>
                        <GridView ColumnHeaderContainerStyle="{StaticResource ColumnHeaderStyle}">
                            <GridViewColumn Header="NAME" Width="220">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" 
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}"
                                                   FontWeight="SemiBold"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="VENDOR" Width="140">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Vendor}" 
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="TYPE" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding TypeLabel}" 
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="PARAMS" Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding NumParameters}" 
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}"
                                                   HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <!-- Preview Section (Placeholder) -->
                <Border Grid.Row="1" Background="{StaticResource AbletonPanel}"
                        BorderBrush="{StaticResource AbletonBorder}" BorderThickness="0,1,0,0"
                        Height="80">
                    <StackPanel Margin="12" VerticalAlignment="Center">
                        <TextBlock Text="PREVIEW" 
                                   Foreground="{StaticResource AbletonTextSecondary}"
                                   FontSize="9" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Text="Select a plugin to preview"
                                   Foreground="{StaticResource AbletonTextSecondary}"
                                   FontSize="10" FontStyle="Italic"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- Bottom Bar: Status & Load Button -->
        <Border Grid.Row="2" Background="{StaticResource AbletonPanel}"
                BorderBrush="{StaticResource AbletonBorder}" BorderThickness="0,1,0,0">
            <Grid Margin="12,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding ScanStatus}"
                           Foreground="{StaticResource AbletonTextSecondary}"
                           VerticalAlignment="Center" FontSize="10"/>

                <Button Grid.Column="1" Content="Load Plugin"
                        Click="OnLoadPlugin_Click"
                        MinWidth="100" Padding="16,6"
                        Background="{StaticResource AbletonOrange}"
                        Foreground="{StaticResource AbletonTextHighlight}"/>
            </Grid>
        </Border>
    </Grid>
</Window>